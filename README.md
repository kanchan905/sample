## run this command in  terminal it will download node module folder and all package that we needede to run this 
   npm install 
## and after downloading node module run cmd in terminal is 
  npm start
